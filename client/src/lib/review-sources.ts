/**
 * UPLIFT DENTAL — Source-backed public feedback links
 * Only link to independently hosted profile pages; do not copy review text,
 * ratings, counts, or photos without a separately verified source record.
 */
export const REVIEW_SOURCES = {
  google: {
    label: "Read current patient feedback on Google",
    url: "https://www.google.com/maps?cid=10268131085528094278",
    attribution: "Verified Google Business Profile",
  },
} as const;
