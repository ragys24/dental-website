[build]
  command = "pnpm install --no-frozen-lockfile && pnpm run build"
    publish = "dist/public"

      [build.environment]
          NODE_VERSION = "20"

          [[redirects]]
              from = "/*"
                  to = "/index.html"
                      status = 200
