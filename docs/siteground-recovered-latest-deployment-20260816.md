# Recovered Latest SiteGround Deployment Record

- **New rollback backup:** `Pre-Recovered-Latest-Uplift-2026-08-16`, confirmed created in SiteGround at 2026-08-15 11:16 PM local SiteGround display time.
- **Existing rollback backup retained:** `Pre-Uplift-static-release-2026-08-16`.
- **Active host/site ID:** `ragys.sg-host.com` / `Smd6MlozZ0VKdz09`.
- **Deployment layout observed:** web document root is `public_html`; protected route documents are maintained in a sibling `route-documents` directory. The document root currently contains the incomplete static release and legacy route-document files.
- **Approved replacement package:** `uplift-latest-recovered-siteground-20260816.zip`, SHA-256 `44df926a6b84cdbcbf47643caa41ce7f95eccc62b72a36d5491eb27b0fe4cf90`.

The fresh backup was confirmed and the deployment runner `siteground-recovered-latest-deploy.php` was created in `public_html`. SiteGround File Manager’s visible file-upload control is not exposed as a browser-addressable file input, so the validated runner will be populated through the built-in editor and will retrieve the checksum-verified release from the temporary direct URL before cleaning itself up.

Post-deployment validation: the recovered homepage is live and includes the restored latest-version specialist content. However, `/patient-portal` returned the shared 404 despite the page, sitemap entry, private route document, and Apache dispatch rule all being present in the package. The deployed `route-document.php` requires `ROUTE_DOCUMENT=1` or `REDIRECT_ROUTE_DOCUMENT=1`; SiteGround does not appear to preserve this environment flag under the deployed rewrite rule. A small handler-only patch is required so it recognizes the original rewritten request URI while retaining denial of direct `/route-document.php` access.

Live post-swap validation via the public canonical host succeeded for the recovered routes. `https://upliftdental.com/patient-portal?verify=recovered-20260816-0640` returned the canonical title `Patient Portal | Online Booking & Records | Uplift Dental` and full Patient Portal content. `https://upliftdental.com/our-specialists?verify=recovered-20260816-0641` returned `Meet Our Dental Specialists | Uplift Dental & Orthodontics` and the restored six-specialist content. The atomic deployment retained prior release rollback directories `public_html.pre-recovered-latest-20260816-0639` and `route-documents.pre-recovered-latest-20260816-0639` on SiteGround.

Server-side validation through the newly deployed SiteGround document root confirmed HTTP 200 for `/patient-portal`, `/our-specialists`, `/community-outreach`, `/dentures`, `/sitemap.xml`, and `/robots.txt`. The obsolete `siteground-recovered-latest-deploy.php` path returns HTTP 404. The deployed sitemap contains 70 canonical URL entries. A release-root scan found no `manus-storage` or `manus.space` references.

Google Search Console’s canonical sitemap detail (accessed 2026-08-16) shows the sitemap was last read on 2026-08-14 before the recovered latest release. It discovered 70 pages and reports 27 `Invalid date` instances. The historical `www` sitemap shows 50 errors and 93 discovered pages; the legacy `/sitemap_index.xml` record remains unavailable. The current sitemap will be checked and corrected before requesting a new Search Console fetch.

Sitemap remediation completed 2026-08-16. All 27 time-bearing `lastmod` values were converted to valid date-only ISO values without changing the 70 canonical URLs. Google Search Console accepted the resubmitted canonical sitemap, listed it as submitted and last read on 2026-08-15, with status `Success` and 70 discovered pages. The prior 27 invalid-date errors are therefore resolved for the canonical sitemap; the older `www` and `sitemap_index.xml` entries are historical records not used by the current canonical robots directive.

Synthetic post-migration audit run from the sandbox’s non-browser network received SiteGround edge HTTP 202 CAPTCHA challenges for every sampled URL, with 2.9–3.3 second challenge responses and `X-Robots-Tag: noindex`. These results are excluded from content-uptime/performance scoring because the authenticated browser successfully rendered restored routes and Google Search Console successfully fetched the canonical sitemap. They do confirm SiteGround’s edge challenge is active for this data-center probe; Googlebot access is not challenged in the same way, as shown by Search Console’s successful sitemap read.

Google Search Console’s Core Web Vitals report shows insufficient Chrome UX Report usage data for both mobile and desktop in the last 90 days. A PageSpeed Insights homepage assessment similarly reports no field data; no laboratory metric summary was returned in the completed report view. Google Search Console’s HTTPS report is positive: zero non-HTTPS URLs, 23 HTTPS URLs, and no issues detected in the last 90 days. These reports establish that field-performance scoring is not yet available because of traffic volume, not because a post-migration HTTPS failure has been detected.

